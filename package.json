{
  "name": "leludo",
  "version": "0.7.0",
  "description": "Ludo board game — browser + Android via Capacitor",
  "private": true,
  "scripts": {
    "icons": "node scripts/gen-app-icons.mjs && capacitor-assets generate --android",
    "version:sync": "node scripts/sync-android-version.mjs",
    "android:prepare": "python3 cache-bust.py && npm run version:sync && cap sync android",
    "android:open": "npm run android:prepare && cap open android",
    "android:run": "npm run android:prepare && cap run android"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.3.4",
    "@resvg/resvg-js": "^2.6.2",
    "sharp": "^0.34.5"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/app": "^8.1.0",
    "@capacitor/core": "^8.3.4",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2"
  }
}
